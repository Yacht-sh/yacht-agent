# Contributing

## Dev Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r agent/requirements.txt
```

## Tests

Run lint/security checks before submitting changes.

## Releasing

Update agent version metadata in code/docs and tag a release when publishing a new agent image.
